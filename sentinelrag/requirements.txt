rich>=13.0
python-dotenv>=1.0
pytest>=7.0

# Optional: only needed to enable real LLM answers (GEMINI_API_KEY or
# BAI_API_KEY). The pipeline runs fully in deterministic STUB mode
# without either configured. DeepSeek (via chat.b.ai) uses the `requests`
# dependency below directly, since its API is OpenAI-compatible over
# plain HTTP -- no separate SDK needed.
google-genai>=2.0

# Web API: backend, auth, and admin document uploads
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
bcrypt>=4.0
PyJWT>=2.8
requests>=2.31

# Real document ingestion (PDF / DOCX)
pypdf>=4.0
python-docx>=1.1

# Optional: semantic retrieval (RetrievalAgent falls back to keyword-only
# matching if this isn't installed or its local index can't be created).
# Uses Chroma's bundled default embedding model -- local, no API key,
# though it downloads a small model file from Hugging Face on first use.
chromadb>=0.5.0
