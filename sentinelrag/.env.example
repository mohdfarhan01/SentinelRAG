# Optional. Get a free key at https://aistudio.google.com/apikey
# Leave unset to run the whole pipeline in deterministic STUB mode
# (no network calls, no cost, no key required).
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
